{
  "name": "anvil-language",
  "description": "Experimental compiler & runtime for Anvil programming language",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:w": "jest --watchAll",
    "build": "tsc",
    "compile": "ts-node ./src/runner.ts ./test/input.anvil"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "commander": "^2.20.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.0",
    "jest": "^24.7.0",
    "ts-jest": "^24.0.1",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
